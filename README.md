# Bunker Dra. Yadira (Vite + React + TS)

## Requisitos
- Node.js + npm

## Variables de entorno
Crear un `.env` local basado en `.env.example`.

Importante: no versionar tokens/secretos en el repo (ni en `.md`).

## Desarrollo
1. `npm install`
2. `npm run dev`
